# UPDATE 02.07.: -> Issue #1: map is read correctly but not drawn correctly (screen is rotated and mirrored)
                 -> Issue #2: Player drawing is faulty (tiles missing after player left, player disappering behind floor)
                 
                 -> fixed get_map_depth
                 -> wall works
                 -> keyhook works

# TODO: Finish map validation functions (check items etc.)
# TODO: test val_exit/val_col functionality
